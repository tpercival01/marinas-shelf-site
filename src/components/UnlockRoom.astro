<section class="unlocks">
  <div class="container unlocks-grid">
    <div class="unlock-visual">
      <div class="screen-frame">
        <img
          src="/images/bookroom-stamp-book.jpg"
          alt="Bookroom Stamp Book screen showing collected reading milestone stamps and unclaimed stamps"
          loading="lazy"
        />
      </div>
    </div>

    <div class="unlock-copy">
      <p class="eyebrow">Collect your stamps</p>

      <h2 class="section-title">
        Let your reading<br />
        leave a mark.
      </h2>

      <p class="lead">
        Reading milestones become part of your Stamp Book.
      </p>

      <p>
        Finish books, reread old favourites and build your library to collect
        stamps that mark the shape of your reading life. Decorations are
        separate, so you can arrange the room freely from the start.
      </p>

      <div class="unlock-points">
        <article>
          <span>01</span>

          <div>
            <h3>Read</h3>
            <p>
              Build your history naturally by finishing books and reaching
              reading milestones.
            </p>
          </div>
        </article>

        <article>
          <span>02</span>

          <div>
            <h3>Collect</h3>
            <p>
              Milestones add stamps to your Stamp Book as your reading history
              grows.
            </p>
          </div>
        </article>

        <article>
          <span>03</span>

          <div>
            <h3>Look back</h3>
            <p>
              Your collected stamps become another record of the books, habits
              and moments that shaped your library.
            </p>
          </div>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .unlocks {
    padding: 120px 0;
    background: var(--color-walnut-900);
    color: var(--color-text-light);
  }

  .unlocks-grid {
    display: grid;
    grid-template-columns: 360px minmax(0, 1fr);
    gap: clamp(70px, 10vw, 140px);
    align-items: center;
  }

  .unlock-visual {
    display: flex;
    justify-content: center;
  }

  .screen-frame {
    width: 330px;
    overflow: hidden;
    border: 1px solid rgb(183 154 98 / 20%);
    border-radius: 36px;
    background: var(--color-cream-50);
    box-shadow: 0 38px 80px rgb(0 0 0 / 28%);
  }

  .screen-frame img {
    display: block;
    width: 100%;
    height: auto;
  }

  .unlock-copy {
    max-width: 640px;
  }

  .unlock-copy .eyebrow {
    color: var(--color-brass);
  }

  .unlock-copy .lead {
    margin: 32px 0 0;
    font-family: var(--font-display);
    font-size: 25px;
    line-height: 1.5;
  }

  .unlock-copy > p:not(.eyebrow):not(.lead) {
    max-width: 550px;
    margin: 20px 0 0;
    color: var(--color-text-muted);
    font-size: 17px;
  }

  .unlock-points {
    margin-top: 48px;
    border-top: 1px solid rgb(245 239 227 / 12%);
  }

  .unlock-points article {
    display: grid;
    grid-template-columns: 48px 1fr;
    gap: 18px;
    padding: 22px 0;
    border-bottom: 1px solid rgb(245 239 227 / 12%);
  }

  .unlock-points article > span {
    padding-top: 4px;
    color: var(--color-brass);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.14em;
  }

  .unlock-points h3 {
    margin: 0;
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 400;
  }

  .unlock-points p {
    margin: 6px 0 0;
    color: var(--color-text-muted);
    font-size: 14px;
  }

  @media (max-width: 850px) {
    .unlocks-grid {
      grid-template-columns: 310px minmax(0, 1fr);
      gap: 55px;
    }

    .screen-frame {
      width: 300px;
    }
  }

  @media (max-width: 700px) {
    .unlocks {
      padding: 80px 0;
    }

    .unlocks-grid {
      grid-template-columns: 1fr;
      gap: 55px;
    }

    .unlock-copy {
      order: -1;
    }

    .screen-frame {
      width: min(320px, 82vw);
    }
  }
</style>
