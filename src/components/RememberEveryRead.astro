<section class="memories">
  <div class="container memories-grid">
    <div class="memories-copy">
      <p class="eyebrow">Remember every read</p>

      <h2 class="section-title">
        A finished book<br />
        is not the end of it.
      </h2>

      <p class="lead">
        Keep the history around a book, not just the fact that you finished it.
      </p>

      <p>
        Bookroom preserves when you read, how you rated it and the private
        memories you want to keep alongside that reading.
      </p>

      <div class="memory-points">
        <article>
          <span>Reading history</span>
          <p>
            Start dates, finish dates and ratings stay attached to the reading
            that created them.
          </p>
        </article>

        <article>
          <span>Private Shelf Memories</span>
          <p>
            Keep personal notes about a read without turning them into public
            reviews or including them in shared shelf images.
          </p>
        </article>

        <article>
          <span>Rereads stay distinct</span>
          <p>
            Read the same book again and Bookroom creates another record rather
            than overwriting the first.
          </p>
        </article>
      </div>
    </div>

    <div class="memory-visual">
      <div class="screen-frame">
        <img
          src="/images/bookroom-reading-history.jpg"
          alt="Bookroom book detail screen showing two separate reading records, reading dates, a five-star rating and a Shelf Memory"
          loading="lazy"
        />
      </div>
    </div>
  </div>
</section>

<style>
  .memories {
    padding: 120px 0;
    background: var(--color-cream-50);
  }

  .memories-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 360px;
    gap: clamp(70px, 10vw, 140px);
    align-items: center;
  }

  .memories-copy {
    max-width: 640px;
  }

  .memories-copy .lead {
    margin: 32px 0 0;
    color: var(--color-text-dark);
    font-family: var(--font-display);
    font-size: 24px;
    line-height: 1.5;
  }

  .memories-copy > p:not(.eyebrow):not(.lead) {
    max-width: 550px;
    margin: 20px 0 0;
    color: var(--color-text-dark-muted);
    font-size: 17px;
  }

  .memory-points {
    margin-top: 48px;
    border-top: 1px solid var(--color-cream-200);
  }

  .memory-points article {
    display: grid;
    grid-template-columns: 180px minmax(0, 1fr);
    gap: 28px;
    padding: 22px 0;
    border-bottom: 1px solid var(--color-cream-200);
  }

  .memory-points span {
    font-family: var(--font-display);
    font-size: 19px;
  }

  .memory-points p {
    margin: 0;
    color: var(--color-text-dark-muted);
    font-size: 14px;
  }

  .memory-visual {
    display: flex;
    justify-content: center;
  }

  .screen-frame {
    width: 330px;
    overflow: hidden;
    border: 1px solid var(--color-cream-200);
    border-radius: 36px;
    background: var(--color-cream-50);
    box-shadow: 0 32px 75px rgb(43 28 22 / 14%);
  }

  .screen-frame img {
    display: block;
    width: 100%;
    height: auto;
  }

  @media (max-width: 850px) {
    .memories-grid {
      grid-template-columns: minmax(0, 1fr) 310px;
      gap: 55px;
    }

    .screen-frame {
      width: 300px;
    }
  }

  @media (max-width: 700px) {
    .memories {
      padding: 80px 0;
    }

    .memories-grid {
      grid-template-columns: 1fr;
      gap: 55px;
    }

    .memory-visual {
      width: min(100%, 410px);
      margin-inline: auto;
    }

    .memory-points article {
      grid-template-columns: 1fr;
      gap: 8px;
    }

    .screen-frame {
      width: min(320px, 82vw);
    }
  }
</style>
