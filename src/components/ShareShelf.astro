<section class="sharing">
  <div class="container sharing-grid">
    <div class="sharing-copy">
      <p class="eyebrow">Share your shelf</p>

      <h2 class="section-title">
        Share the room,<br />
        not the database.
      </h2>

      <p class="lead">
        When you want to share your reading life, Bookroom turns it into a
        polished portrait made to save or send.
      </p>

      <p>
        Share your current bookcase, Five Books and other reading views without
        exposing the private notes behind them.
      </p>

      <div class="sharing-points">
        <article>
          <span>01</span>

          <div>
            <h3>Made for sharing</h3>
            <p>
              Bookroom creates editorial-style images from the reading views
              you choose, rather than asking you to share a database or list.
            </p>
          </div>
        </article>

        <article>
          <span>02</span>

          <div>
            <h3>Private stays private</h3>
            <p>
              Shelf Memories are not included in generated sharing images.
            </p>
          </div>
        </article>

        <article>
          <span>03</span>

          <div>
            <h3>You decide where it goes</h3>
            <p>
              Sharing is initiated by you through the normal iOS sharing
              interface.
            </p>
          </div>
        </article>
      </div>
    </div>

    <div class="sharing-visual">
      <div class="share-frame">
        <img
          src="/images/bookroom-five-books-share.jpg"
          alt="Bookroom Five Books share image showing five selected book covers with their titles and authors"
          loading="lazy"
        />
      </div>
    </div>
  </div>
</section>

<style>
  .sharing {
    padding: 120px 0;
    background: var(--color-cream-100);
  }

  .sharing-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 430px;
    gap: clamp(70px, 10vw, 140px);
    align-items: center;
  }

  .sharing-copy {
    max-width: 640px;
  }

  .sharing-copy .lead {
    margin: 32px 0 0;
    color: var(--color-text-dark);
    font-family: var(--font-display);
    font-size: 24px;
    line-height: 1.5;
  }

  .sharing-copy > p:not(.eyebrow):not(.lead) {
    max-width: 550px;
    margin: 20px 0 0;
    color: var(--color-text-dark-muted);
    font-size: 17px;
  }

  .sharing-points {
    margin-top: 48px;
    border-top: 1px solid var(--color-cream-200);
  }

  .sharing-points article {
    display: grid;
    grid-template-columns: 48px 1fr;
    gap: 18px;
    padding: 22px 0;
    border-bottom: 1px solid var(--color-cream-200);
  }

  .sharing-points article > span {
    padding-top: 4px;
    color: var(--color-burgundy);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.14em;
  }

  .sharing-points h3 {
    margin: 0;
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 400;
  }

  .sharing-points p {
    margin: 6px 0 0;
    color: var(--color-text-dark-muted);
    font-size: 14px;
  }

  .sharing-visual {
    display: flex;
    justify-content: center;
  }

  .share-frame {
    width: 420px;
    overflow: hidden;
    border: 1px solid var(--color-cream-200);
    border-radius: 24px;
    background: #f7f0e2;
    box-shadow: 0 34px 75px rgb(43 28 22 / 16%);
  }

  .share-frame img {
    display: block;
    width: 100%;
    height: auto;
  }

  @media (max-width: 850px) {
    .sharing-grid {
      grid-template-columns: minmax(0, 1fr) 350px;
      gap: 55px;
    }

    .share-frame {
      width: 350px;
    }
  }

  @media (max-width: 700px) {
    .sharing {
      padding: 80px 0;
    }

    .sharing-grid {
      grid-template-columns: 1fr;
      gap: 55px;
    }

    .share-frame {
      width: min(100%, 390px);
    }
  }
</style>
